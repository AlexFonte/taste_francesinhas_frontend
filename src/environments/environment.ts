export const environment = {
  production: false,
  // URL relativa: el dev server hace de proxy hacia localhost:8080 (ver proxy.conf.json).
  // Asi funciona tanto desde el PC como desde el movil en la misma red.
  apiUrl:     '/tastefrancesinhas',
  appVersion: '1.0.0',
};